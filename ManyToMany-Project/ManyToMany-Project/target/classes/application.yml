spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Gopal
    username: Gopal
    password: Gopal
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9092